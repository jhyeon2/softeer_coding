import sys
input = sys.stdin.readline

M, N, K = map(int,input().split())
menu = ''.join(list(input().rstrip().split()))
button = ''.join(list(input().rstrip().split()))

if menu in button:
    print('secret')
else:
    print('normal')   
